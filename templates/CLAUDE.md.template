# {{PROJECT_NAME}}

## Project

{{PROJECT_DESCRIPTION}}
<!-- 2-4 sentences: what the project does, who uses it, what problem it solves.
     Be specific: "a React app" < "a customer-facing dashboard for X used by Y" -->

## Stack

- **Runtime**: {{RUNTIME_AND_VERSION}}
- **Framework**: {{FRAMEWORK_AND_VERSION}}
- **Testing**: {{TEST_FRAMEWORK}}
- **Linting**: {{LINTER}}
<!-- Add: UI library, state management, ORM, migration tool, task queue, etc. -->
<!-- Include exact versions — Claude uses them to avoid deprecated APIs -->

## Commands

```bash
{{INSTALL_COMMAND}}        # install dependencies
{{DEV_COMMAND}}            # start dev server / run locally
{{BUILD_COMMAND}}          # production build (if applicable)
{{TEST_COMMAND}}           # run all tests
{{TEST_WATCH_COMMAND}}     # tests in watch mode (if available)
{{LINT_COMMAND}}           # lint + type check
{{LINT_FIX_COMMAND}}       # auto-fix lint issues
```
<!-- Add any project-specific commands: generate:api, migrate, seed, storybook, etc. -->

## Architecture

```
{{KEY_DIRECTORY_1}}/    ← {{DESCRIPTION_1}}
{{KEY_DIRECTORY_2}}/    ← {{DESCRIPTION_2}}
{{KEY_DIRECTORY_3}}/    ← {{DESCRIPTION_3}}
```
<!-- Focus on non-obvious structure. Note auto-generated directories. -->
<!-- Example: "lib/api/ — generated API client, don't edit by hand" -->

## Conventions

- **{{CONVENTION_TOPIC_1}}**: {{CONVENTION_DETAIL_1}}
- **{{CONVENTION_TOPIC_2}}**: {{CONVENTION_DETAIL_2}}
<!-- Examples:
  - Imports: use @/ alias (not relative ../../)
  - Components: named exports only, one per file
  - Styling: Tailwind only, no CSS modules
  - API calls: always go through lib/api/, never raw fetch
  - Dates: use date-fns, never new Date() arithmetic directly
-->

## Environment

Copy `{{ENV_EXAMPLE_FILE}}` to `{{ENV_FILE}}` and fill in:
- `{{ENV_VAR_1}}` — {{ENV_VAR_1_DESCRIPTION}}
- `{{ENV_VAR_2}}` — {{ENV_VAR_2_DESCRIPTION}}
<!-- Note where to find secrets (1Password, team channel, etc.) -->
<!-- Flag which vars are optional vs. required for local dev -->

## Gotchas

<!-- This is the highest-value section. Aim for 5+ specific, hard-won facts.
     Answer these questions for each category that applies:

  PACKAGE MANAGER — What is the only approved package manager? What breaks if someone uses the wrong one?
  AUTO-GENERATED CODE — Which files/dirs are overwritten on regeneration? What triggers regeneration? What NEVER to edit?
  ENVIRONMENT SAFETY — What commands must never run against prod? What target/schema/branch checks exist?
  PORT CONFLICTS — What ports does local dev use? Any services that must be started first?
  FLAKY TESTS — Any known flaky tests? How to re-run reliably? Any seed or fixture ordering dependency?
  ORDER-DEPENDENT STEPS — What must happen before what? What fails silently if steps are out of order?
  ASYNC PATTERNS — What looks synchronous but isn't? Common deadlock/race condition traps?

  See examples/*/CLAUDE.md for real patterns from similar projects.
-->

- **{{PACKAGE_MANAGER}}**: {{detail — e.g., "Only pnpm. npm install will create a package-lock.json and break CI."}}
- **{{AUTO_GENERATED}}**: {{detail — e.g., "src/api/ is generated by openapi-typescript. Never edit by hand — run pnpm run generate:api."}}
- **{{ENV_SAFETY}}**: {{detail — e.g., "Never run dbt run --target prod locally. Use --target dev."}}
- **{{PORT_CONFLICTS}}**: {{detail — e.g., "Dev server on :3000, API on :8000, Postgres on :5432. All must be free."}}
- **{{FLAKY_TESTS}}**: {{detail — e.g., "Integration tests in tests/e2e/ require SEED_DB=1 on first run."}}
- **{{OTHER}}**: {{any other hard-won insight that would surprise a new contributor}}

<!-- OPTIONAL SECTIONS — add if relevant: -->

<!--
## Testing

{{TESTING_STRATEGY}}
- Unit tests: {{UNIT_TEST_LOCATION}}
- Integration tests: {{INTEGRATION_TEST_LOCATION}}
- Fixtures: {{FIXTURES_LOCATION}}
- Mocks: {{MOCKS_APPROACH}}
-->

<!--
## Deployment

- Dev: {{DEV_DEPLOY_PROCESS}}
- Staging: {{STAGING_DEPLOY_PROCESS}}
- Production: {{PROD_DEPLOY_PROCESS}}
- Branch → environment mapping: {{BRANCH_ENV_MAPPING}}
-->

<!--
## External Services

| Service | Purpose | Local mock |
|---------|---------|------------|
| {{SERVICE_1}} | {{PURPOSE_1}} | {{MOCK_1}} |
-->
